---
description: Unity 2D Topdown + TopDown Engine（TDE）專案的長駐規範（效能/穩定/可維護）
alwaysApply: true
---

# Unity 2D Topdown（TDE 優先）長駐規則

你現在是在協助一個 **Unity 2022.3.62f3c1** 的 **2D Topdown** 專案，而且專案已使用下列外掛/套件：

- **TopDown Engine (TDE)**：`Assets/TopDownEngine`
- **Shapes**：`Assets/ThirdParty/Shapes`
- **A* Pathfinding Project**：`Packages/com.arongranberg.astar`

本規則等同「trigger: always_on」：**不管使用者打開哪個檔案、問哪件事，你都要遵守下面的要求。**

---

## 1) 語言與框架（硬性規定）

- **所有腳本一律用 C#（`.cs`）**，不要用其他語言。
- **優先使用 Unity 2022.3.62f3c1 的最新穩定 API**。
- **嚴禁使用過時/舊系統**：
  - 不要用舊版 Input Manager（`Input.GetAxis` / `Input.GetKey` 那套）來做核心輸入流程；**優先用 Unity Input System**（如果專案已啟用）。
  - 不要用已標記過時（Obsolete）的 API；如果你非得碰到，請直接說「這段是過時 API，不建議」，並給替代方案。
- **MonoBehaviour 欄位**：
  - 預設用 **`[SerializeField] private`**，不要用 `public` 欄位把資料全部暴露在 Inspector。
  - 需要只讀但要顯示在 Inspector 的，用「**自訂 Inspector / PropertyDrawer**」或「**只顯示不讓改**」的方式處理（不要用 `public` 充數）。

---

## 2) 講話方式（你輸出的文字風格）

- **一律用繁體中文**，而且要 **白話、口語、好懂**（不要堆艱澀術語）。
- 需要解釋時要講清楚「為什麼這樣做比較穩、比較快」，不要只貼碼。
- 只要你在產生/修改腳本，請附上：
  - **怎麼掛到 Unity 物件上**
  - **Inspector 要填什麼**
  - **跟 TDE 現有系統怎麼接起來**

---

## 3) TopDown Engine（TDE）優先原則（最重要）

- **能用 TDE 現成機制做的，絕對不要自己造輪子。**
- 在你要「新增功能」之前，先做這件事：
  - **先去查 TDE 既有能力**（看 `Assets/TopDownEngine` 裡的 scripts、prefabs、demo scene 的做法）
  - 如果 TDE 已經有對應的 Character Ability / Weapon / Health / AI / Pooling 方案：**直接用它**
- **真的做不到**才寫自訂腳本，而且你必須在輸出裡明講：
  - **「TDE 原生做不到的點是什麼」**
  - **「因此我才補一個相容的自訂腳本」**
  - **「會不會跟 TDE 衝突、怎麼避免衝突」**

---

## 4) vibe code 風格：Inspector 乾淨、避免誤改

你寫的腳本要做到「用起來順、別讓人亂改到爆炸」：

- **把不必要、容易誤改、或在選取/禁用時會造成問題的選項藏起來**
  - 用 `private` + `[SerializeField]` 控制曝光面
  - 必要時做 **CustomEditor**：只顯示真正需要調的參數、其餘鎖定或隱藏
  - 避免把整個系統的內部狀態（runtime state）暴露給 Inspector 被手改
- **不要做「吞錯誤」或「默默回退」的防呆**：
  - 不要 `try { } catch { }` 然後當沒事
  - 不要遇到錯就改用另一條「可能能跑但不對」的流程
  - 取而代之：給使用者一個 **可開關（toggle）的錯誤日誌**，而且訊息要「看了就知道怎麼修」

---

## 5) 錯誤日誌（With toggle）的規範

- 每個系統/腳本若可能遇到設定錯誤，都要提供：
  - `EnableLogs` / `EnableVerboseLogs` 這種 **明確的 bool 開關**
  - log 內容要包含：**物件名稱、元件名稱、缺什麼/哪裡不對、建議修法**
- 預設不要瘋狂刷 log（尤其是 Update 內），但當使用者打開 toggle，就要能快速定位問題。

---

## 6) 效能與穩定（假設同時 1000+ 敵人）

使用者明確說了：場上會同時超過 **1000 個敵人**，而且 **敵人/特效全部都用物件池**。所以你寫的任何東西都要先過這個門檻：

- **物件生成**：
  - 任何會大量出現的東西（子彈、特效、敵人、掉落物）都必須走 **物件池**（優先用 TDE / More Mountains 既有 pooling 機制）。
- **每幀成本**：
  - 能不用 `Update()` 就別用；改成事件驅動、定時 tick、或把昂貴邏輯分批處理。
  - 嚴禁每幀做 `Find*` / `GetComponent` 反覆查找（需要就快取）。
  - 避免 GC Alloc（例如每幀 `new`、LINQ、字串拼接），特別是 1000+ 物件時會直接爆。
- **物理/碰撞**：
  - 一律用合理的 Layer/Mask，把碰撞矩陣設好；不要「全部互撞」。
  - 能用 NonAlloc API 就用（例如需要掃描大量碰撞時）。
- **特效**：
  - 視覺效果也要池化（包含粒子、命中特效、UI 飄字等）。

---

## 7) 外掛相容性（Shapes / A*）

- **Shapes**：
  - 只拿來做視覺呈現（線條/圖形/UI 特效等），不要把遊戲邏輯綁死在 Shapes 的渲染行為上。
- **A* Pathfinding Project**：
  - 路徑計算是大戶，1000+ AI 時你要非常小心：
    - 避免所有敵人同一帧同時重算路徑
    - 盡量做節流/分批更新
  - 如果 TDE 本身已經有「適用的 AI 移動/追蹤」解法，優先用 TDE；A* 只在 TDE 不夠用時再上。

---

## 8) 產出腳本時你必須做的事（固定交付格式）

只要你要生成 C# 腳本，請一併提供：

- **使用步驟**：怎麼新增、怎麼掛、Inspector 參數怎麼填、跟 TDE 的哪個元件/Ability/Weapon 接在一起
- **風險檢查清單**：這段程式最可能壞在哪、最可能造成效能災難的點是什麼（直接點名）
- **效能考量**：你是怎麼避免 GC、怎麼避免每幀成本、池化如何做（優先用 TDE）

---

## 9) 底線

- 你要自始至終都選「**最穩、最好維護、最不會卡**」的做法；使用者不在意工作量，只在意 **優化與穩定**。
- 如果你不確定 TDE 是否已有能力支援某功能：**先查 TDE 原始碼/範例做法，再決定要不要自訂**。

